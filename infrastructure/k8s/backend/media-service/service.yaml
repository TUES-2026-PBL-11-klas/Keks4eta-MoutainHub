apiVersion: v1
kind: Service
metadata:
  name: media-service
  namespace: mountainhub
  labels:
    app: media-service
spec:
  selector:
    app: media-service
  type: ClusterIP
  ports:
    - name: http
      port: 5000
      targetPort: 5000
