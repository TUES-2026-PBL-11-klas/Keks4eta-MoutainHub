apiVersion: v1
kind: Service
metadata:
  name: review-service
  namespace: mountainhub
  labels:
    app: review-service
spec:
  selector:
    app: review-service
  type: ClusterIP
  ports:
    - name: http
      port: 5000
      targetPort: 5000
